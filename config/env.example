# RAD Gateway Environment Configuration
# Copy this file to /opt/radgateway01/config/env for production

# Server Configuration
RAD_LISTEN_ADDR=:8090

# Database Configuration
# For PostgreSQL (recommended for production):
RAD_DB_DRIVER=postgres
RAD_DB_DSN=postgresql://radgateway_user:radgateway_secure_password_2024@localhost:5432/radgateway?sslmode=disable

# For SQLite (fallback, no CGO required):
# RAD_DB_DRIVER=sqlite
# RAD_DB_DSN=radgateway.db

# Connection Pool Settings (PostgreSQL only)
RAD_DB_MAX_OPEN_CONNS=10
RAD_DB_MAX_IDLE_CONNS=3
RAD_DB_CONN_MAX_LIFETIME=5m

# API Keys (comma-separated key:value pairs)
# Format: name:secret,name2:secret2
# In production, use Infisical for secret management
RAD_API_KEYS=test:rad_test_key_12345

# Retry Configuration
RAD_RETRY_BUDGET=2

# Infisical Configuration (for secrets management)
# INFISICAL_API_URL=http://localhost:8080
# INFISICAL_TOKEN_FILE=/opt/radgateway01/config/infisical-token
# INFISICAL_PROJECT_ID=your-project-id
# INFISICAL_ENVIRONMENT=dev

# Logging
# LOG_LEVEL=info
# LOG_FORMAT=json
